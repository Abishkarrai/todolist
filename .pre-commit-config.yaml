repos:
  - repo: local
    hooks:
      - id: staged-secret-scan
        name: staged secret scan
        entry: powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts/Invoke-SecretScan.ps1
        language: system
        pass_filenames: false
        stages: [pre-commit]
